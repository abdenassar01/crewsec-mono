[phases.setup]

nixPkgs = ["...", "nodejs_22"]

[phases.install]

cmd = "pnpm install --frozen-lockfile"

[phases.build]

cmd = "pnpm --filter web build"

[start]

cmd = "pnpm --filter web start"

[variables]

NODE_ENV = "production"
NPM_FLAGS = "--prefix=/"
